global:
  scrape_interval: 10s
  evaluation_interval: 10s

rule_files:
  - "/etc/prometheus/alerts.yml"

scrape_configs:
  - job_name: "featureflow_api"
    static_configs:
      - targets: ["api:8000"]
    metrics_path: "/metrics"
